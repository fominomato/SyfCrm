{% extends 'ComprasBundle::base.html.twig' %}

{% block title %}Elever-Página Configuracoes{% endblock %}

{% block body %}
    <div class="container">
    <h1>EbRegional list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Idregional</th>
                <th>Nome</th>
                <th>Ativo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>
                    <a href="{{ path('configuracao_regional_show', { 'id': entity.idRegional }) }}">{{ entity.idRegional }}</a>
                </td>
                <td>{{ entity.nome }}</td>
                <td>{{ entity.ativo }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('configuracao_regional_show', { 'id': entity.idRegional }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('configuracao_regional_edit', { 'id': entity.idRegional }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('configuracao_regional_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>

    </div>
    {% endblock %}
