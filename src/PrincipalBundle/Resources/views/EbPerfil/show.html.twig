{% extends 'ComprasBundle::base.html.twig' %}

{% block title %}Elever-Página Configuracoes{% endblock %}

{% block body %}
    <div class="container">
    <h1>EbPerfil</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Idperfil</th>
                <td>{{ entity.idPerfil }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Ativo</th>
                <td>{{ entity.ativo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('configuracao_perfil') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('configuracao_perfil_edit', { 'id': entity.idPerfil }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
    </div>
{% endblock %}
